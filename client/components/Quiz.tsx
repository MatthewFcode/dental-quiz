import {useState} from 'react'

function Quiz() {
  // import the data to the component
  // map through the questions options for the answers and add a radio button for each of the options
  return <div></div>
}

export default Quiz () {
 // const [index, setImageIndex] = useState(0)


  // function handleImageToggle () {

  // }

  return (
    <div> 
    </div>
  )
}
